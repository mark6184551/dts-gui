import { FastifyInstance, RouteShorthandOptions, FastifyReply } from 'fastify'

const job_state_route = (server: FastifyInstance, opts: RouteShorthandOptions, done: (error?: Error) => void) => {

    
}


export default job_state_route